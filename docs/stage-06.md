# Stage 6

Input: usable Stage 2, usable Stage 3, usable Stage 5, applicable active corrections, and source lookup when needed.

Output: `analysis/06-next-directions.md`.

Each direction stores an ID, related gap, next question, reason, expected impact, required input, and priority. Include run metadata required by `output-format.md`.

Review policy: `none`. After successful execution, review status is `not_required`.

If a direction cannot be traced to an existing gap, do not save it as formal output.
