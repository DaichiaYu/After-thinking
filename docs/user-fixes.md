# User fixes

User fixes must remain effective on later reruns unless the user explicitly replaces or retires them.

Each fix needs a stable ID and a target object or field. Later runs must load applicable active fixes before analysis.
